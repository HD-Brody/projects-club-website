DATABASE_URL=postgresql://user:password@localhost:5432/dbname
SECRET_KEY=replace_with_a_secret
JWT_SECRET_KEY=optional_jwt_secret

# Flask debug mode (set to false in production)
FLASK_DEBUG=true

# CORS allowed origins (comma-separated, no spaces)
# In production set this to your real domain, e.g.: https://yourclub.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5173

# ================================
# SMTP Email Configuration
# ================================
# IMPORTANT: SMTP_FROM_EMAIL must match SMTP_USER for most email providers
# Providers block spoofing attempts where From address differs from authenticated user

# Option 1: Gmail (Development/Testing)
# Requires: App Password (https://support.google.com/accounts/answer/185833)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-16-char-app-password
# SMTP_FROM_EMAIL=your-email@gmail.com
# SMTP_FROM_NAME=UofT Projects Club

# Option 2: UofT Email (Recommended for Production)
# Use the official club email for professional appearance
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=projectsclub@utoronto.ca
# SMTP_PASSWORD=club-email-password
# SMTP_FROM_EMAIL=projectsclub@utoronto.ca
# SMTP_FROM_NAME=UofT Projects Club

# Option 3: Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASSWORD=your-password
# SMTP_FROM_EMAIL=your-email@outlook.com
# SMTP_FROM_NAME=UofT Projects Club

# Option 4: SendGrid (Production - Recommended for scale)
# Free tier: 100 emails/day
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM_EMAIL=noreply@yourclub.com
# SMTP_FROM_NAME=UofT Projects Club
